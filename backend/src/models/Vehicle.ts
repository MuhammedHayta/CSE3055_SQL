class Vehicle {
    ID: number;
    LicensePlate: string;

    constructor(ID: number, LicensePlate: string) {
        this.ID = ID;
        this.LicensePlate = LicensePlate;
    }
}

export default Vehicle;
